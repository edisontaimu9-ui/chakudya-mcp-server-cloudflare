{
  "name": "chakudya-mcp-server-cloudflare",
  "version": "1.0.0",
  "description": "MCP server exposing the Chakudya Nutrition Registry (CNR) API as MCP tools over Streamable HTTP — Cloudflare Workers edition. Same tools as the Render/Express version; deployed separately.",
  "license": "MIT",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "tail": "wrangler tail"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.30.0",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250906.0",
    "typescript": "^5.5.3",
    "wrangler": "^4.20.0"
  }
}
